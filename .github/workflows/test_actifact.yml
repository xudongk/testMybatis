name: test actifact

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: upload file
      uses: actions/upload-artifact@v2
      with:
        name: artifact
        path: ./*
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
    # - name: down file
    #   uses: actions/download-artifact@v2
    #   with:
    #     name: artifact
    - name: Delete artifact
      run: |
        response=$(curl -s https://api.github.com/repos/xudongk/testMybatis/actions/runs/${{ github.run_id }}/artifacts)
        artifact_id=$(echo $response | awk -F'"id":' '{print $2}' | awk -F',' '{print $1}')
        echo $artifact_id
        # curl \
        #   -X DELETE \
        #   -H "Authorization: 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" \
        #   -H "Accept: application/vnd.github.v3+json" \
        #   "https://api.github.com/repos/xudongk/testMybatis/actions/artifacts/artifact"
    # - name: check file
    #   run: |
    #     ls
    
