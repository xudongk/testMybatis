name: test actifact

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: upload file
      uses: actions/upload-artifact@v2
      with:
        name: artifact
        path: ./*
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: down file
      uses: actions/download-artifact@v2
      with:
        name: artifact
    - name: Delete artifact
      run: |
        curl \
          -X DELETE \
          -H "Authorization: Bearer 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" \
          -H "Accept: application/vnd.github.v3+json" \
          "https://api.github.com/repos/xudongk/testMybatis/actions/artifacts/artifact"
    - name: check file
      run: |
        ls
    
